ОТЧЕТ
при помощи автоматического тестирования:
Были протестированы модели:
    Создание объектов, связи, поля
Формы: 
    Валидация регистрации (пароли и уникальность имени)
API-представления( views):
    Регистрация, подача заявки
Авторизация и роли:
    Разграничения доступа (гость, пользователь, админ)
Безопасность:
    Хеширование паролей
Админ панель:
    Экспорт данных в xlxs
ЧЕКЛИСТ ПО РУЧНОМУ ТЕСТИРОВАНИЮ
Критерии функциональности: 
 + Все ссылки и кнопки работают: Нет "битых" ссылок, ведущих на 404.+
Формы:
    +Валидация работает корректно (подсвечиваются ошибки при неправильном вводе).
    +После успешной отправки формы происходит ожидаемое действие (сохранение, редирект).
    +Полный цикл основной услуги (напр., оформление заказа) выполняется от начала до конца без ошибок.
Разделы прав доступа:
    +Гость не может получить доступ к страницам, требующим авторизации.
    +Обычный пользователь не может получить доступ к админ-панели.
    +Администратор имеет полный доступ.
Критерии удобства использования - юзабилити:
    +Адаптивность: приложение корректно отображается на трех видах устройств (ПК, планшет, смартфон). Элементы не "наезжают" друг на друга, текст читаем.
    + Интуитивно понятный интерфейс: пользователь понимает, как взаимодействовать сприложением без инструкции.
    Все изображения и другие медиафайлы загружаются и отображаются корректно.
    + CSS-стили применены ко всем элементам, нет "голого" HTML.
Критерии надежности:
    +Обработка ошибок: приложение не "падает" при возникновении ошибок (напр., несуществующая страница показывает кастомную 404, а не debug-страницу Django).
    -Устойчивость к неверному вводу: приложение корректно обрабатывает некорректные данные от пользователя.
Критерии производительности:
    +Скорость загрузки: основные страницы загружаются за приемлемое время (не более 2-3 секунд).
    +Нет проблемы N+1 запрос - страницы со списками объектов (каталог товаров, список заказов) не создают сотни запросов к БД.
Критерии безопасности
    +Пароли хешируются - в базе данных пароли пользователей хранятся в виде хешей.
Критерии сопровождения: 
    +Код хорошо отформатирован, имеет понятные названия переменных и функций.
    -Сложная бизнес-логика сопровождается комментариями.
    +Проект имеет несколько осмысленных коммитов с понятными сообщениями.
